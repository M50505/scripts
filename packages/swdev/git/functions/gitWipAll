#!/usr/bin/env bash
# vim:tabstop=2:shiftwidth=2:nu:filetype=sh

gitWipAll ()
{
	local status=1
	local newDir
	local oldDir="${PWD}"
	
	for newDir in *
	do
		if gitNeedsCommit "${newDir}"
		then
			echo "Synchronizing ${newDir}"
			
			cd "${newDir}"

			gitwip

			cd "${oldDir}"
		fi
	done
}

