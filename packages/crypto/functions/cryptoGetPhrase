#!/usr/bin/env bash
# vim:tabstop=2:shiftwidth=2:nu:filetype=sh
cryptoGetPhrase ()
{
	_doc $1 20140322151946 "gets the passphrase for encryption and decryption from a special environment variable" && return 0

	echo ${_c_r_y_p_t_o_p_h_r_a_s_e_}
}
